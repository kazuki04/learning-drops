version: '3'
services:
  react:
    build:
      context: .
      dockerfile: ./docker/react/Dockerfile
    container_name: rok_react_container
    tty: true
    volumes:
      - ./frontend:/app/react
    command: sh -c "cd /app/react  && yarn start"
    ports:
      - 8080:8080
