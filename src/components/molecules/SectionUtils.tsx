import React from "react";
import CreateButton from "../atoms/buttons/CreateButton";

const SectionUtils = () => {
  return (
    <React.Fragment>
      <CreateButton button_word="card" />
    </React.Fragment>
  )
}

export default SectionUtils;
